# Quickstart: validar o fence autorizado

1. Focado: `python3 tests/validate_orchestrator_store_contract.py` (aresta `RESULT_RECORDED → FAILED` só com `diagnostic_ref`; `RESULT_RECORDED → BLOCKED` continua inválido) e `python3 tests/validate_agent_orchestration_contract.py` (`test_activity_fence`: forma órfã cercada e em seguida `TAKEOVER-APPLIED`; forma retida cercada pelo líder vivo e por sucessor com líder terminal; recusas sem autorização, autorização de outro contexto/atividade/run, líder vivo que não é o solicitante, especialista vivo, inconclusivo do especialista, do líder e do sucessor, não observável, hash stale, estado fora das duas formas — todas com Store idêntico em prévia e apply; replay `FENCE-REUSED`; aceite tardio `ACTIVITY-STATE-DIVERGENCE`; salto 2 interrompido e retomado).
2. Suíte: `python3 tests/run_validators.py` (conte pelo marcador `==>`) e `git diff --check`. Sem rede, sem `orca` real; a matriz é ubuntu/windows/macos × Python 3.10/3.13.
3. Distribuição: `python3 tests/validate_distribution.py` com os 8 pontos em 6.0.31 e a seção `## 6.0.31` no `CHANGELOG.md`.
4. Live (fora do aceite desta entrega; FASE-002, depois do ship e da instalação da 6.0.31): prévia read-only sobre `interview-author-001` do work item de origem — `gauntlet-activity-fence . --work-id fix-continuidade-sem-checkpoint-d62bb2a5380d4409ace677fa741af28d --activity-id interview-author-001 --session-ref <sessão do líder GWD> --authorization <bundle>` — conferindo `activity_state=RESULT_RECORDED`, `resource_state=CLOSE_PENDING`, especialista `ctx_2923e0218c89` terminal e o veredicto do líder `orca:ctx_ad48e72ddf4c`, antes de qualquer `--apply`. No X7 a prévia roda na máquina onde o Store vive (EVIDENCE GAP, DQ-0001).
