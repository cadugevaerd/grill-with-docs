<!-- grill-task-files:v1 -->

# Tasks: Fence autorizado de atividade órfã

**Input**: Design documents from `specs/034-fence-autorizado-atividade/`

**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/activity-fence.md, quickstart.md, checklists/release-gate.md

**Tests**: obrigatórios (FR-013, SC-002, SC-005). Hoje nenhum verbo leva a atividade órfã ou de sessão retida a estado terminal, e a spec exige que toda recusa deixe o Store bit a bit igual. Sem os casos novos, nada prova que o fence só abre com prova e autorização exatas, nem que cada recusa escreve zero bytes. Todos determinísticos, offline, sem `orca` real, pelos seams já existentes na suíte.

**Contrato**: `task-files/v1`. Cada tarefa declara `Files` como array JSON de uma linha logo abaixo; tarefa despachável que escreve declara um `Result` em `specs/034-fence-autorizado-atividade/implement/`, também listado em `Files`; tarefa read-only declara `Files` vazio e não tem `Result`. `Files` é a única autoridade de escrita do worker; nenhuma palavra da descrição concede grant. Conflito de `Files` agrupa tarefas no mesmo nó dentro da fase; fases são barreiras.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: pode rodar em paralelo (arquivos disjuntos, sem dependência pendente)
- **[Story]**: US1..US4 do `spec.md`
- Citações `arquivo:linha` conferidas no HEAD `a5ee1da` (código de `plugin/` e `tests/` idêntico ao `39380f7` revisado por `plan-reviewer-003`; `git diff --stat 39380f7 HEAD -- plugin tests` vazio)

## Path Conventions

Repositório existente, sem módulo novo. Arquivos tocados (13): o CLI do core e o contrato de atividade em `grill_core`, dois validadores, os dois headings de documentação sob `plugin/`, os quatro manifests, o validador de distribuição, `README.md` e `CHANGELOG.md` na raiz. Sob o contrato v1 a raiz é grant válido, então os dois arquivos da raiz vão para o mesmo worker do bump. Os resultados por tarefa nascem em `specs/034-fence-autorizado-atividade/implement/`, diretório novo.

Nós esperados do Execution DAG (arquivos disjuntos entre nós da mesma fase, research R11 e R12): **A** = aresta nova + validador do Store (Phase 1); **B** = verbo + validador de orquestração (Phase 2); **C** = documentação + nove arquivos de versão (Phase 2, independente de B); Phase 3 só tem tarefas read-only, sem worker.

---

## Phase 1: Aresta nova no contrato de atividade

**Purpose**: A única mudança de contrato do Store (DQ-0009). Sem ela a forma retida não tem saída para `FAILED`; os testes p2 e p2b da fase seguinte dependem dela, por isso a fase é barreira.

- [ ] T001 [US2] Em `plugin/skills/grill-with-docs/scripts/grill_core/agent_orchestration.py`, acrescentar `FAILED` ao literal `_ACTIVITY_EDGES["RESULT_RECORDED"]` (linha 48), que passa a ser exatamente `{"RESULT_RECORDED", "ACCEPTED", "FAILED"}`; é literal congelado, escrito à mão, nunca derivado de outra tabela (regra de `CLAUDE.md` para `ESSENTIAL` e `SEQUENCE_V*`); `_RESOURCE_EDGES` (49), `_OPERATION_EDGES`, `SPECIALIST_PAIRS`, a policy `agent-orchestration.v1.json` e `RECEIPT_CATEGORIES` do `store.py` não mudam; `diagnostic_ref` obrigatório em `FAILED` já vem de 784-785 e uma atividade `FAILED` com `result_ref`, `result_sha256` e `output_manifest` preenchidos continua válida (762, 770); nenhum outro caminho do CLI percorre a aresta nova, porque `gauntlet-activity --diagnostic` só sai de `DISPATCHED` (grill_workspace.py:6078-6080) e `gauntlet-prepare-switch` grava `FAILED` só de `DISPATCHED` (3952-3953). Em `tests/validate_orchestrator_store_contract.py`, travar a aresta no padrão `invalid(...)` com `ORCHESTRATOR_INVALID` (288-290) sobre as fixtures `ORCHESTRATION_ACTIVITY` e `ORCHESTRATION_RESOURCE` (31-34): (a) atividade `RESULT_RECORDED` com resultado gravado que vai a `FAILED` com `diagnostic_ref` transaciona e conserva `result_ref`, `result_sha256` e `output_manifest` intocados; (b) a mesma transição sem `diagnostic_ref` recusa; (c) `RESULT_RECORDED` para `BLOCKED` continua recusada; (d) `DISPATCHED` para `FAILED` com `diagnostic_ref` continua aceita, provando que a tabela só ganhou um valor (DQ-0009, ADR-0001, FR-007, research R9, data-model seção Aresta nova, quickstart 1)
  Files: ["plugin/skills/grill-with-docs/scripts/grill_core/agent_orchestration.py", "tests/validate_orchestrator_store_contract.py", "specs/034-fence-autorizado-atividade/implement/T001.tasks.json"]
  Result: "specs/034-fence-autorizado-atividade/implement/T001.tasks.json"

**Checkpoint**: `python3 tests/validate_orchestrator_store_contract.py` fecha em exit 0 com os quatro casos novos; `python3 tests/validate_agent_orchestration_contract.py` continua passando sem edição.

---

## Phase 2: Verbo de fence, testes de contrato, documentação e distribuição

**Purpose**: Dois nós disjuntos. Nó B (T002, T003, T004, serial no mesmo par de arquivos) entrega o verbo em três fatias, cada uma com o próprio teste, para que o worker prove cada fatia antes da seguinte. Nó C (T005, T006) entrega a prosa e o bump; não depende de B em código, só da fase anterior.

- [ ] T002 [US1] [US2] [US3] Em `plugin/skills/grill-with-docs/scripts/grill_workspace.py`, registrar o verbo `gauntlet-activity-fence` ao lado de `context_takeover_parser` (7131-7136): `root`, `--work-id` obrigatório, `--activity-id` obrigatório, `--session-ref` obrigatório como em 7134, `--authorization` SEM `required` e default `None` (a omissão tem de sair como `FENCE-AUTHORIZATION-INVALID` com payload, nunca como exit 2 do argparse, lição de `CLAUDE.md` e Finding 2 de plan-reviewer-001), `--expected-sha256` e `--apply`; acrescentar `gauntlet_activity_fence_command` à tabela de dispatch junto de `gauntlet-context-takeover` (7212) e escrevê-la ao lado de `gauntlet_context_takeover_command` (4126-4350), NÃO decorada com `@_gauntlet_authorized` (3393), porque na forma órfã o líder do contexto está terminal e `require_authority` recusaria antes de qualquer checagem (3441-3444). Prévia e apply executam as mesmas checagens na mesma ordem; apply só acrescenta o hash e a mutação (padrão 4130-4134; contrato linha 13; CHK037). Esta fatia entrega os passos 1 a 4 de research R2: (1) `--session-ref` vazio recusa `INVALID-ARGUMENTS` (4137-4138); `store.read_snapshot(root, required=True)`; work item ausente recusa `ORCHESTRATION-MIGRATION-REQUIRED` (4141-4143); atividade ausente recusa `FENCE-ACTIVITY-NOT-FOUND`; `context_id`, `resource_id` (`session_resource_id`), `epoch` e `fence` são lidos da atividade e do contexto no Store, nunca do chamador (CHK038); recurso ausente ou com `kind` diferente de `session` recusa `FENCE-ACTIVITY-STATE`; `operation_id` é `"fence-"` mais os 24 primeiros hex de sha256 de `canonical` (237) do objeto com as chaves `context` e `activity`, determinístico e reencontrável; (2) função auxiliar `_fence_recorded(item, operation_id, activity_id, resource_id, session_ref)` chamada ANTES de estado, provas e hash, porque o hash inclui `activity_state` e o replay parte de atividade já `FAILED`: operação ausente segue; operação `kind` `activity-fence`, `CONFIRMED`, `subject_ids` igual a `[activity_id, resource_id]`, atividade `FAILED` com `diagnostic_ref` igual ao `result_ref` da operação, recurso `CLOSED` e `session_ref` igual a `intended_after.requester.ref` é replay e devolve `FENCE-REUSED` (exit 0, prévia e apply, sem observar, sem reler a autorização e sem comparar hash, como `TAKEOVER-REUSED` em 4148-4156); mesma forma e mesmo solicitante com recurso `CLOSE_PENDING` é retomada: a prévia devolve `FENCE-PREVIEW` com `expected_sha256` igual a `input_sha256` da operação, `hops` 1 e `resume` true, e o apply segue para o caminho de retomada entregue em T004; forma de replay ou de retomada com `session_ref` diferente do solicitante gravado não é nem um nem outro e segue para o passo 3, onde cai em `FENCE-ACTIVITY-STATE` sem escrever (Finding 1 de plan-reviewer-001; DQ-P3 opção A); qualquer outra forma com a operação presente (kind, estado, subjects, atividade ou recurso divergentes) recusa `FENCE-CAS-CONFLICT` com `activity_state`, `resource_state` e `operation_id` relidos no payload; (3) o par (estado da atividade, estado do recurso) fora de `(DISPATCHED, REGISTERED)` e `(RESULT_RECORDED, CLOSE_PENDING)` recusa `FENCE-ACTIVITY-STATE`, as duas únicas formas que o CLI produz (933-939, 887-911, 6097-6106; CHK026); (4) autorização antes das observações por ser leitura local: flag `None` recusa `FENCE-AUTHORIZATION-INVALID` antes de abrir arquivo; senão `load_checkpoint_attestation(root, args.authorization)` (5340-5359) e `attestation._validate_human_authorization(bundle, scope)` (773-781) com `scope` igual a `work_id`, `context_id` e `activity_id` unidos por dois-pontos (ids não admitem esse separador: `_ID` em agent_orchestration.py:40 e `WORK_ID_RE` em store.py:142); toda falha (ausente, ilegível, malformado, `schema` errado, `scope` divergente, `decision` diferente de `APPROVED`, refs fora de `FREE_REF_RE` e `SHA256_RE`) vira o único código `FENCE-AUTHORIZATION-INVALID`, como `ABANDON-AUTHORIZATION-INVALID` (5174-5183); `content_sha256` só na forma (DQ-0013 A; SGD-40); o bundle fica verbatim para `intended_after.authorization`. Em `tests/validate_agent_orchestration_contract.py`, abrir `test_activity_fence` no padrão de `test_context_takeover` (2257-2645) com os seams desta fatia: fixture `orchestration_fixture.offline_leader`; invocador `fence(work_id, activity_id, session_ref, authorization, *tail)` no molde do invocador `takeover`; `guarded_run` estendido para receber um mapa `{dispatch_id: raw}` e devolver o raw do id que segue `--dispatch` em `cmd` (agent_runtime.py:1273), porque o fence observa dois dispatches, mantendo `None` como proibição de observar (2281-2292); `assert_refused_and_unwritten` para o fence, comparando `content_sha256` do Store antes e depois em prévia E apply com `--expected-sha256` de 64 zeros (2297-2307; Finding 2 de checklist-reviewer-001); seed da forma órfã pelo padrão 1245-1266 sem `record_activity_result` e com recurso `REGISTERED`, com observação do especialista por `boundary` sobre um dispatch próprio (orchestration_fixture.py:55-58) para que `identity.owner_dispatch` seja um id nu; seed da forma retida acrescentando `record_activity_result` (942) e `resource["state"] = "CLOSE_PENDING"` (1257-1260); escritor de bundle de autorização JSON relativo ao root com `schema`, `scope` canônico, `decision`, `authorized_by`, `receipt_ref` e `content_sha256`. Casos desta fatia, todos sob `guarded_run(None)` para provar que nenhuma observação acontece antes da autorização: (n1) flag `--authorization` omitida, arquivo ausente, ilegível, JSON malformado e `decision` diferente de `APPROVED` recusam `FENCE-AUTHORIZATION-INVALID`; (n2) `scope` com outro `context_id`, com outro `activity_id` e com um `run_id` recusam `FENCE-AUTHORIZATION-INVALID`; (n2d) `scope` com outro `work_id` recusa `FENCE-AUTHORIZATION-INVALID`; (n8) atividade `ACCEPTED`, `VERIFIED` e `FAILED` recusam `FENCE-ACTIVITY-STATE`; (n8b) os três pares que o CLI não produz, semeados direto, `(RESULT_RECORDED, REGISTERED)`, `(DISPATCHED, CLOSE_PENDING)` e `(RESULT_RECORDED, CLOSED)`, recusam `FENCE-ACTIVITY-STATE`; (n0) `--activity-id` inexistente recusa `FENCE-ACTIVITY-NOT-FOUND` (FR-001, FR-003, FR-013, US3 AS1, AS2, research R1, R2, R5, contrato linhas 4, 9, 10, 13, DQ-0006, DQ-0013)
  Files: ["plugin/skills/grill-with-docs/scripts/grill_workspace.py", "tests/validate_agent_orchestration_contract.py", "specs/034-fence-autorizado-atividade/implement/T002.tasks.json"]
  Result: "specs/034-fence-autorizado-atividade/implement/T002.tasks.json"
- [ ] T003 [US1] [US2] [US3] Em `plugin/skills/grill-with-docs/scripts/grill_workspace.py`, completar `gauntlet_activity_fence_command` com os passos 5 a 7 de research R2, na mesma ordem em prévia e apply: (5) observação do especialista por `_takeover_observation(root, context["runtime"], ref)` (1565-1609) com `ref` igual a `"orca:" + owner_dispatch` quando `identity.owner_dispatch` do recurso for string e `None` caso contrário (guarda `isinstance` de 3697; `"orca:" + None` levantaria `TypeError` antes do adapter, Finding 5 de plan-reviewer-002); mapeamento idêntico ao takeover (4159-4166): `not_observable` recusa `FENCE-NOT-OBSERVABLE` nomeando a referência; não terminal com `status` em `{dispatched, running}` recusa `FENCE-SPECIALIST-ACTIVE` qualquer que seja a liveness; qualquer outro não terminal (status ausente ou não string, dispatch não correlacionado, resposta ilegível, sem `ORCA_TERMINAL_HANDLE` absorvido em `indeterminate` por agent_runtime.py:1284-1285) recusa `FENCE-SPECIALIST-UNPROVEN`; terminal é `status` fora de `{dispatched, running}`, ou `capabilityRevokedAt` não nulo, ou liveness `exited` por `agent_status` (1286-1291); (6) observação do líder por `context["leader"]["session_ref"]` pela mesma função: `not_observable` recusa `FENCE-NOT-OBSERVABLE`; terminal faz do solicitante o sucessor, provado por `_session_readiness(root, context["runtime"], args.session_ref, work_id=args.work_id)` (1612-1655, a mesma chamada do takeover em 4181), cujas recusas (`LEADER-AUTHORITY-UNPROVEN`, `STYLE-SCOPE-CONFLICT`, diagnóstico de apresentação) sobem inalteradas, e `requester` vira `{"role": "successor", "ref", "sha256", "incarnation"}` da readiness; vivo (`status` em `{dispatched, running}` sem revogação) exige `args.session_ref` igual ao `session_ref` do líder, senão `FENCE-LEADER-ACTIVE`, e com igualdade reobserva por `_require_current_leader(root, args.work_id, context, args.session_ref)` (1658-1671), cuja divergência sobe como `LEADER-AUTHORITY-UNPROVEN`, com `requester` `{"role": "current-leader"}` e `ref`, `sha256`, `incarnation` do `context["leader"]`; nem terminal nem vivo recusa `FENCE-LEADER-UNPROVEN`; `leader.verdict` só assume `terminal` ou `active`; (7) `expected = store.jcs_sha256(...)` sobre exatamente as chaves de data-model seção Hash da prévia (`work_id`, `context_id`, `activity_id`, `activity_state`, `resource_id`, `resource_state`, `to_session_ref` igual a `args.session_ref`, `specialist` com `verdict` e `reference`, `leader` com `verdict` e `reference`, `authorization` com `scope`, `decision`, `authorized_by`, `receipt_ref`, `content_sha256`), sem digest de resposta nem `snapshot.revision` (motivo em 4247-4259); a prévia devolve `FENCE-PREVIEW` com os campos de sucesso do contrato linha 15 menos `store_revision`, mais `expected_sha256` e `hops` (2 para `(DISPATCHED, REGISTERED)`, 1 para `(RESULT_RECORDED, CLOSE_PENDING)`), sem escrever; apply com hash divergente recusa `FENCE-INPUTS-STALE` (4268-4270), inclusive quando só `--session-ref` mudou desde a prévia; apply com hash coincidente segue para a mutação entregue em T004. Em `tests/validate_agent_orchestration_contract.py`, acrescentar a `test_activity_fence` os casos desta fatia, cada negativo por `assert_refused_and_unwritten` em prévia e apply, com `takeover_show(dispatch_id, status=, revoked=, liveness=)` (62-77) roteado pelo `guarded_run` por dispatch: (n3) líder `status` `dispatched` com liveness `live` de `agent_status` e `--session-ref` diferente do líder recusa `FENCE-LEADER-ACTIVE`; (n3c) líder terminal e `_session_readiness` mockada para levantar (como em 263) recusa com o código da readiness, nada escrito; (n4) especialista `dispatched` com liveness `live` recusa `FENCE-SPECIALIST-ACTIVE`; (n5) especialista com `status` ausente e liveness `unverifiable` (shape de 2516), dispatch não correlacionado (2514) e resposta ilegível (2512) recusam `FENCE-SPECIALIST-UNPROVEN`, NUNCA com `status` `dispatched`, porque esse shape é vivo por definição (Finding 1 de plan-reviewer-003); (n5b) os mesmos três shapes no líder recusam `FENCE-LEADER-UNPROVEN`; (n5c) sem `ORCA_TERMINAL_HANDLE` (2508-2511) recusa `FENCE-SPECIALIST-UNPROVEN`; (n5d) especialista `status` `dispatched`, `capabilityRevokedAt` nulo e liveness `unverifiable` recusa `FENCE-SPECIALIST-ACTIVE`, travando a distinção entre vivo e indeterminado; (n6) recurso com `identity.owner_dispatch` nulo recusa `FENCE-NOT-OBSERVABLE`, sem `TypeError`; (n7) apply com hash de 64 zeros e apply com o hash da prévia mas `--session-ref` diferente recusam `FENCE-INPUTS-STALE`; (pv1) prévia da forma órfã com especialista `completed` e capability revogada e líder `completed` devolve `FENCE-PREVIEW` com `hops` 2, `requester.role` `successor`, `evidence` de ambos os dispatches com `observation_ref`, `observation_sha256`, `dispatch_status`, `liveness` e `verdict`, e `content_sha256` do Store igual antes e depois; (pv2) prévia da forma retida com líder vivo e `--session-ref` do líder devolve `FENCE-PREVIEW` com `hops` 1 e `requester.role` `current-leader`, sem escrever; a ordem é provada por (n1) sob `guarded_run(None)` já em T002 e por (n4) com o líder nem sequer consultado, que se prova entregando ao `guarded_run` só o raw do especialista (FR-002, FR-004, FR-005, FR-013, US3 AS3, AS4, AS5, US2 AS1, AS2, research R3, R4, R6, R11, contrato linhas 7, 8, 11, 12, 16, DQ-0008, specify-reviewer-002 F2, CHK009, CHK022)
  Files: ["plugin/skills/grill-with-docs/scripts/grill_workspace.py", "tests/validate_agent_orchestration_contract.py", "specs/034-fence-autorizado-atividade/implement/T003.tasks.json"]
  Result: "specs/034-fence-autorizado-atividade/implement/T003.tasks.json"
- [ ] T004 [US1] [US2] [US3] [US4] Em `plugin/skills/grill-with-docs/scripts/grill_workspace.py`, completar `gauntlet_activity_fence_command` com a mutação de research R7, o caminho de retomada de R2 e a classificação do `except`: salto 1, sempre, por `store.transact(root, mutate)` (padrão 4300-4343) com guarda de revisão `document["revision"] == snapshot.revision` (4309-4310) e guarda de estado (atividade no estado da prévia com o mesmo `session_resource_id`, recurso no estado da prévia, `operation_id` ausente em `operations`), qualquer divergência levanta `store.StoreError`; grava a operação `CONFIRMED` com exatamente a forma da tabela de data-model seção Operação (`operation_id`, `kind` `activity-fence`, `context_id`, `fence` igual ao `fence` do líder do contexto, `subject_ids` `[activity_id, resource_id]`, `input_sha256` igual a `expected`, `expected_before` com `context_id`, `activity_id`, `activity_state`, `resource_id`, `resource_state`, `intended_after` com `reason` `activity-fence`, `activity_state` `FAILED`, `resource_state` `CLOSED`, `evidence`, `requester`, `authorization` verbatim, `successor` `attempt-2-as-new-activity` e `applied_at` RFC3339 UTC, `idempotency_key` igual a `operation_id`, `result_ref` no formato `activity-fence` barra `operation_id` ponto json, `result_sha256` igual a `store.jcs_sha256` de `evidence`, `requester` e `authorization`, `observation_ref` igual a `result_ref`, `error` nulo); a atividade vai a `FAILED` com `diagnostic_ref` igual a `result_ref` (first-bound de `None`, 1537-1539) e todo o resto intocado, inclusive `result_ref`, `result_sha256`, `output_manifest`, `accepted_by_context`, `acceptance_ref` e `review_verdict`; o recurso recebe o receipt `{ref: "orca:" + owner_dispatch + ":fence", sha256: digest da observação do especialista}` acrescentado se ausente (refs únicos 1054-1058; append 1550-1553), `last_observation` igual a esse ref (1066-1068), `operation_id` igual a `operation_id` (1002-1003, 1442), `result_acceptance_ref` continua `None` e `state` vai a `CLOSE_PENDING` se estava `REGISTERED` ou a `CLOSED` se estava `CLOSE_PENDING` (arestas em agent_orchestration.py:49, uma transição por `transact`, store.py:1626-1627); salto 2, só quando o salto 1 deixou `CLOSE_PENDING` ou na retomada, por um segundo `transact` que NÃO reusa `snapshot.revision` (o salto 1 carimbou revisão nova, 1624) e guarda só por estado: operação presente, `CONFIRMED`, `kind` `activity-fence`, `input_sha256` igual a `expected`; atividade `FAILED` com `diagnostic_ref` igual a `result_ref`; recurso `CLOSE_PENDING` vai a `CLOSED`; recurso já `CLOSED` levanta `store.StoreError(store.STATE_DIVERGENCE, "fence already completed")` e NUNCA devolve o documento sem mudança, porque `transact` sempre carimba e escreve (1624-1631); qualquer outra divergência levanta `StoreError`; no `except` de qualquer salto o CLI relê o snapshot e classifica por `_fence_recorded`: forma de replay com o mesmo solicitante devolve `FENCE-REUSED`; operação reencontrada sob outro solicitante, ou qualquer outra forma, recusa `FENCE-CAS-CONFLICT` com `activity_state`, `resource_state` e `operation_id` relidos (Findings 3 e 4 de plan-reviewer-002; contrato linha 14); retomada: apply sobre `(FAILED, CLOSE_PENDING)` com operação presente, mesmo solicitante e `--expected-sha256` igual a `input_sha256` da operação reexecuta só a prova do solicitante conforme `intended_after.requester.role` (`successor` por `_session_readiness`; `current-leader` por `_require_current_leader`), prova recusada sobe sem escrever, hash diferente recusa `FENCE-INPUTS-STALE`, e então executa só o salto 2 (Finding 4 de plan-reviewer-002; CHK036); sucesso devolve `FENCE-APPLIED` com `work_id`, `context_id`, `activity_id`, `operation_id`, `activity_state` `FAILED`, `resource_state` `CLOSED`, `evidence`, `requester` e `store_revision` (contrato linha 15). Em `tests/validate_agent_orchestration_contract.py`, fechar `test_activity_fence` com os casos desta fatia: (p1) forma órfã com especialista `completed` revogado observado por `orca:` mais `owner_dispatch` e líder `completed`: `FENCE-PREVIEW` sem escrita, `FENCE-APPLIED` com `store_revision`, atividade `FAILED` com `diagnostic_ref` igual ao `result_ref` da operação, recurso `CLOSED` com o receipt de sufixo `:fence`, `last_observation` nesse ref e `operation_id` preenchido, operação `CONFIRMED` com autorização verbatim, `evidence` dos dois dispatches e `requester.role` `successor` cujos `ref`, `sha256` e `incarnation` são iguais aos de `_session_readiness` (como 2348-2353); em seguida `gauntlet-context-takeover` pela mesma sessão devolve `TAKEOVER-PREVIEW` e `TAKEOVER-APPLIED`, e o recurso cercado não aparece em `preserved_resources` do payload (chave pública em 4070, 4121, 4348; nit (a) de plan-reviewer-003); (p2) forma retida com especialista `completed` revogado e liveness `unverifiable`, líder vivo e `--session-ref` do próprio líder: `FENCE-APPLIED` num salto, `requester.role` `current-leader`, `result_ref` e `result_sha256` intocados, `acceptance_ref`, `accepted_by_context` e `review_verdict` nulos, `result_acceptance_ref` do recurso nulo; em seguida `gauntlet-cleanup --context-id` desse contexto lista o recurso cercado com `verdict` `UNKNOWN` e `code` `SESSION-CLOSE-UNPROVEN`, veredicto geral `UNKNOWN`, exit 2, e `content_sha256` do Store igual antes e depois (4452-4455, 4474-4478; DQ-0014 A; SGD-41; CHK029); (p2b) forma retida com líder terminal e sucessor: idem com `requester.role` `successor`; (p3) replay em prévia e em apply com hash de 64 zeros pelo mesmo solicitante devolve `FENCE-REUSED` sob `guarded_run(None)`, `content_sha256` igual (2640-2645); (p3b) replay sobre `(FAILED, CLOSED)` e retomada sobre `(FAILED, CLOSE_PENDING)` com `--session-ref` diferente de `intended_after.requester.ref` recusam `FENCE-ACTIVITY-STATE` sem escrever, e nesse segundo estado `gauntlet-context-takeover` por outra sessão continua admitido em prévia, porque `CLOSE_PENDING` não é `UNKNOWN` (3668; nit (c) de plan-reviewer-003); (p4) `gauntlet-activity --phase accept` pelo líder vivo sobre a atividade cercada recusa `ACTIVITY-STATE-DIVERGENCE` (6074-6075), e `--phase accept --diagnostic` sobre uma atividade `RESULT_RECORDED` não cercada recusa `ACTIVITY-STATE-DIVERGENCE` (6078-6080), provando que só o fence percorre a aresta nova (SC-003, CHK024); (p5) salto 2 interrompido, por mock de `store.transact` que levanta após o primeiro `transact` ou por seed direto de `(FAILED, CLOSE_PENDING)` mais operação `CONFIRMED`, depois nova prévia com `resume` true e apply com o `input_sha256` da operação: a prova do solicitante roda antes do salto 2 e o resultado é `FENCE-APPLIED` com recurso `CLOSED`; reaplicar devolve `FENCE-REUSED`; variante pós-salto 1: sobre `(FAILED, CLOSE_PENDING)` mais operação, mover o recurso para `PRESERVED` por `transact` e aplicar com o hash da operação recusa `FENCE-CAS-CONFLICT` com `activity_state`, `resource_state` e `operation_id` no payload, e o Store fica igual ao estado pós-salto 1, não ao anterior à primeira invocação, que é a única saída do verbo com código de recusa e efeito próprio já gravado (Finding 3 de checklist-reviewer-001; Finding 4 de plan-reviewer-001); (p6) na forma retida com líder vivo, depois de `FENCE-APPLIED`, `gauntlet-prepare-switch` em prévia pelo mesmo líder não recusa `CONTINUITY-ACTIVE-WORK` por causa da atividade cercada (3974-3976), como já é invocado offline em 275, 1303 e 2385; se o setup da troca exigir mais do que o valor do caso, asserir ao menos que `_continuity_quiescence` (3654-3672) não lista a atividade em `active` (FR-009; Finding 4 de checklist-reviewer-001; CHK025); (n9) `read_snapshot` mockada para devolver `revision - 1` no apply, único seam válido (2626-2637), recusa `FENCE-CAS-CONFLICT` na guarda de revisão do salto 1 com Store igual; escrita real interposta entre prévia e apply que não toca o par passa com `FENCE-APPLIED` e escrita que toca o par recusa por estado ou por hash, nunca por CAS (Finding 1 de plan-reviewer-002); (n10) retomada sem prova do solicitante: seed direto de `(FAILED, CLOSE_PENDING)` mais operação com `requester.role` `successor`, apply com o `input_sha256` da operação e `_session_readiness` mockada para levantar recusa com o código da readiness, recurso continua `CLOSE_PENDING`, Store igual; variante `requester.role` `current-leader` com `context["leader"]` divergente do `observe()` recusa `LEADER-AUTHORITY-UNPROVEN`, Store igual; contraprova: replay sobre `(FAILED, CLOSED)` com `_session_readiness` mockada para levantar continua `FENCE-REUSED`, porque o replay é read-only e não observa (FR-006, FR-007, FR-008, FR-009, FR-010, FR-011, FR-012, FR-013, US1 AS2, AS3, US2 AS1, AS2, AS3, US4 AS1, AS2, SC-001, SC-003, SC-004, research R2, R7, R8, R10, R11, contrato linhas 14, 15, 17, 18, 19, 20, data-model seções Recurso, Operação e Transições, DQ-0009, DQ-0010, DQ-0014, CHK027, CHK036)
  Files: ["plugin/skills/grill-with-docs/scripts/grill_workspace.py", "tests/validate_agent_orchestration_contract.py", "specs/034-fence-autorizado-atividade/implement/T004.tasks.json"]
  Result: "specs/034-fence-autorizado-atividade/implement/T004.tasks.json"
- [ ] T005 Em `plugin/skills/grill-with-docs/references/session-protocol.md`, acrescentar depois do parágrafo de `gauntlet-context-takeover` (linha 85) e antes do parágrafo de `--released-source` (linha 87) um parágrafo próprio de `gauntlet-activity-fence`, com a sintaxe completa do verbo (`ROOT --work-id ID --activity-id ID --session-ref REF --authorization PATH`, `--apply --expected-sha256 SHA` só na efetivação), as duas formas admitidas (`DISPATCHED` órfã e `RESULT_RECORDED` de sessão retida), as duas provas terminais obtidas do ambiente (dispatch do especialista por `orca:` mais `owner_dispatch` e dispatch do líder da atividade), a regra de autoridade (líder vivo: só o líder corrente exato; líder terminal: sucessor com a própria sessão provada por readiness), a autorização humana exata `human-authorization/v1` com escopo work item, contexto e atividade, o efeito (atividade `FAILED` com `diagnostic_ref`, recurso `CLOSED` com receipt correlacionado, operação `activity-fence` `CONFIRMED`), a frase de que o resultado cercado nunca é aceito nem herdado e de que a reexecução nasce como atividade nova, e a ressalva de que `gauntlet-cleanup` não reconhece esse fechamento nesta versão e o reporta como `SESSION-CLOSE-UNPROVEN` sem bloquear takeover, switch, phase-turn ou ship (DQ-0014, SGD-41); silêncio, expiry e observação inconclusiva recusam. Em `plugin/skills/grill-with-docs/SKILL.md`, acrescentar ao lado da frase do takeover (linha 94) uma frase de que atividade de especialista órfã ou de sessão retida é encerrada sem aceite por `gauntlet-activity-fence`, sob prova terminal do especialista e do líder e autorização humana exata, e que nada do resultado cercado migra. Os headings de versão dos dois arquivos ficam para T006 (FR-001, FR-007, FR-011, research R12, contrato linhas 19 e 20)
  Files: ["plugin/skills/grill-with-docs/references/session-protocol.md", "plugin/skills/grill-with-docs/SKILL.md", "specs/034-fence-autorizado-atividade/implement/T005.tasks.json"]
  Result: "specs/034-fence-autorizado-atividade/implement/T005.tasks.json"
- [ ] T006 Bump 6.0.30 para 6.0.31 (fix, PATCH) nos nove arquivos que o gate exige, hoje todos em 6.0.30: `plugin/.claude-plugin/plugin.json` (linha 3), `plugin/.codex-plugin/plugin.json` (linha 3), `.claude-plugin/marketplace.json` (linha 11), `.agents/plugins/marketplace.json` (linha 7), a constante `VERSION` em `tests/validate_distribution.py` (linha 8), o heading `# Grill with Docs v6.0.31` em `plugin/skills/grill-with-docs/SKILL.md` (linha 6), o heading `# Protocolo de sessão v6.0.31` em `plugin/skills/grill-with-docs/references/session-protocol.md` (linha 1), o heading `**v6.0.31` em `README.md` (linha 3) e uma seção nova `## 6.0.31` em `CHANGELOG.md` acima de `## 6.0.30` (linha 3), com uma entrada `Fix:` que nomeie `gauntlet-activity-fence`, as duas formas cercadas, as provas terminais do especialista e do líder, a readiness do solicitante, a autorização humana exata, o não-aceite do resultado e a aresta nova `RESULT_RECORDED` para `FAILED`; `tests/validate_distribution.py` (41-43) exige exatamente um heading `## 6.0.31`, por isso são nove arquivos e não os oito de FR-014; nenhum outro byte desses arquivos muda; conferir com `python3 tests/validate_distribution.py` imprimindo `distribution: OK` (FR-014, Constituição cláusulas Bump obrigatório e Release obrigatória por versão, research R12, quickstart 3, Findings 1 e 5 de checklist-reviewer-001, CHK006)
  Files: ["plugin/.claude-plugin/plugin.json", "plugin/.codex-plugin/plugin.json", ".claude-plugin/marketplace.json", ".agents/plugins/marketplace.json", "tests/validate_distribution.py", "plugin/skills/grill-with-docs/SKILL.md", "plugin/skills/grill-with-docs/references/session-protocol.md", "README.md", "CHANGELOG.md", "specs/034-fence-autorizado-atividade/implement/T006.tasks.json"]
  Result: "specs/034-fence-autorizado-atividade/implement/T006.tasks.json"

**Checkpoint**: nó B: `python3 tests/validate_agent_orchestration_contract.py` fecha em exit 0 com `test_activity_fence` completo e nenhum caso existente editado para passar; nó C: `python3 tests/validate_distribution.py` imprime `distribution: OK`.

---

## Phase 3: Verificação read-only

**Purpose**: Provar a suíte inteira e a fronteira de FR-015 sem escrever byte algum. Sem worker; o líder aceita as duas tarefas nesta posição.

- [ ] T007 [P] Rodar `python3 tests/run_validators.py` e registrar exit 0 e a contagem de validadores pelo marcador `==>`; rodar em foco `python3 tests/validate_orchestrator_store_contract.py` e `python3 tests/validate_agent_orchestration_contract.py` e registrar exit 0; rodar `git diff --check` e registrar saída vazia; nenhum teste toca a rede nem exige `orca`, `specify`, `node` ou `backlogctl` reais; a matriz de integração (ubuntu, windows, macos com Python 3.10 e 3.13) é conferida no PR, não aqui (SC-005, quickstart 1 e 2, CHK020, CHK030)
  Files: []
- [ ] T008 [P] Conferir a fronteira de FR-015 e FR-014 por leitura: `git diff --stat` da base do work item até o HEAD corrente não toca `.specify/memory/constitution.md`, `WORKFLOW.md`, nada sob `plugin/skills/grill-with-docs/assets/` (registries, catálogos, policy `agent-orchestration.v1.json`, snapshot de confiança), `plugin/skills/grill-with-docs/scripts/grill_core/attestation.py`, `plugin/skills/grill-with-docs/scripts/grill_core/agent_runtime.py` nem `plugin/skills/grill-with-docs/scripts/grill_core/workflow_versions.py`; o sha256 de `.specify/memory/constitution.md` continua `54d5522b18e43efa05311dbf13ed79694b79ccfcb01509384b3572b2d5667569`; `python3 tests/validate_distribution.py` imprime `distribution: OK` e `grep -rn 6.0.30` nos nove arquivos de T006 devolve vazio, exceto o heading histórico `## 6.0.30` do `CHANGELOG.md`; registrar o resultado no relatório do líder, não no repositório (FR-014, FR-015, Finding 1 de checklist-reviewer-001, CHK035)
  Files: []

**Checkpoint**: suíte verde, diff limpo, fronteira intocada; `PLAN_ONLY_STOP` continua sendo o fim do ciclo de fix.

---

## Dependencies

```text
Phase 1  T001 (nó A)                                   barreira
         ↓
Phase 2  nó B: T002 → T003 → T004 (serial, mesmo par de arquivos)
         nó C: T005 → T006 (serial, headings compartilhados)      B ∥ C
         ↓                                              barreira
Phase 3  T007 ∥ T008 (read-only, sem worker)
```

- T002, T003 e T004 conflitam em `grill_workspace.py` e no validador de orquestração, então formam um nó só; a ordem é a ordem das tarefas.
- T005 e T006 conflitam nos dois headings de documentação; T006 é o último porque o validador de distribuição só fecha com os nove arquivos.
- Nó B depende de T001 em execução (p2 e p2b percorrem a aresta nova); a barreira de fase resolve.
- Nó C não depende de B em código; depende só da barreira da Phase 1, como todo nó da Phase 2.
- Phase 3 não tem tarefa despachável; as duas tarefas são aceitas pelo líder na ordem das tarefas depois de B e C convergirem.

## Parallel opportunities

- Phase 2: nó B e nó C em arquivos disjuntos (research R11, R12; plan-reviewer-003 seção 6).
- Phase 3: T007 e T008 são leituras independentes.

## Independent test criteria

- **US1**: forma órfã cercada, recurso `CLOSED` com receipt, takeover admitido em seguida, nada do resultado migra (T004 p1, p3).
- **US2**: forma retida cercada pelo líder vivo e pelo sucessor com líder terminal; aceite tardio recusado; aresta nova travada (T001, T004 p2, p2b, p4).
- **US3**: cada negativo recusa com o próprio código e deixa o Store igual em prévia e apply; a matriz negativa é a de research R11 (n0, n1, n2, n2d, n3, n3c, n4, n5, n5b, n5c, n5d, n6, n7, n8, n8b, n9, n10, p3b), não a amostra de SC-002 (T002, T003, T004; Finding 2 de checklist-reviewer-001).
- **US4**: replay devolve reuso sem segundo evento; hash desatualizado recusa sem efeito; retomada só com prova (T004 p3, p5, n7, n9, n10).

## Rastreabilidade

| Requisito | Tarefas |
|---|---|
| FR-001 verbo único, prévia por padrão | T002, T003, T005 |
| FR-002 prévia sem escrita, hash fechado | T003 (pv1, pv2, n7) |
| FR-003 autorização exata, um código | T002 (n1, n2, n2d) |
| FR-004 prova terminal do ambiente | T003 (n4, n5, n5c, n5d, n6) |
| FR-005 autoridade pela observação do líder | T003 (n3, n3c, n5b, pv2), T004 (p2b, n10) |
| FR-006 duas formas | T002 (n8, n8b), T004 (p1, p2) |
| FR-007 estado final não aceito | T001, T004 (p2, p4) |
| FR-008 sessão fechada com recibo | T004 (p1, p2, p5) |
| FR-009 quiescência e herança inalterada | T004 (p1, p6) |
| FR-010 rastreabilidade da operação | T004 (p1) |
| FR-011 attempt 2 como atividade nova | T004 (`successor` em p1), T005 |
| FR-012 reuso e hash divergente | T004 (p3, p3b, p5, n9), T003 (n7) |
| FR-013 recusa sem efeito, dado nomeado | T002, T003, T004 (todo negativo em prévia e apply) |
| FR-014 bump | T006, T008 |
| FR-015 fronteira intocada | T008 (verificado pelo grant do DAG e por diff) |
| SC-001 | T004 (p1, p2, p2b) |
| SC-002 | T002, T003, T004 (matriz de R11) |
| SC-003 | T004 (p4) |
| SC-004 | T004 (p3, p5) |
| SC-005 | T007 |

## Insumos obrigatórios incorporados

- plan-reviewer-003 Finding 1 (shape de n5 e n5b): T003, com n5d como trava adicional.
- plan-reviewer-003 nit (a) (`preserved_resources`): T004 p1. Nit (c) (solicitante substituído entre saltos): T004 p3b. Nits (b) e (d) tocam prosa de plan e PLAN-CONTEXT, fora deste grant.
- checklist-reviewer-001 Finding 1 (FR-014 e FR-015 sem SC): T006 e T008. Finding 2 (matriz negativa de R11): T002, T003, T004 e a seção de critérios acima. Finding 3 (Store pós-salto 1 em p5): T004. Finding 4 (prepare-switch após fence): T004 p6. Finding 5 (nove arquivos nomeados): T006.
- CHK036 (retomada), CHK037 (paridade prévia e apply) e CHK038 (ids lidos do Store) sugeridos pelo revisor: cobertos por T002 passo 1 e 2, T003 e T004 (p5, n10, p3b); o arquivo de checklist fica fora deste grant.

## Implementation strategy

MVP = T001 mais T002, T003 e T004: o fence existe, recusa sem prova ou autorização e destrava a sucessão com Store íntegro. Nó C acompanha por obrigação constitucional de bump e leva a prosa; Phase 3 é a prova final sem escrita. O ciclo de fix termina em `PLAN_ONLY_STOP`; nenhum destes passos publica.
