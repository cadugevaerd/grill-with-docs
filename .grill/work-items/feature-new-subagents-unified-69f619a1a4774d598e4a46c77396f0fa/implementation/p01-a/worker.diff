diff --git a/plugin/skills/grill-with-docs/assets/agent-orchestration.v1.json b/plugin/skills/grill-with-docs/assets/agent-orchestration.v1.json
new file mode 100644
index 0000000..ac679a8
--- /dev/null
+++ b/plugin/skills/grill-with-docs/assets/agent-orchestration.v1.json
@@ -0,0 +1,69 @@
+{
+  "schema": "grill-agent-orchestration-policy/v1",
+  "policy_version": "1",
+  "contract": "grill-agent-orchestration/v1",
+  "entrypoint": {
+    "kind": "canonical-skill",
+    "rule": "resolve-and-invoke",
+    "supplement_is_not": ["alias", "macrostep", "registry-entry", "skill-invocation"]
+  },
+  "roles": {
+    "codex": {
+      "leader_recommendation": "Sol",
+      "author": {"model": "gpt-6-astra", "reasoning_effort": "xhigh"},
+      "reviewer": {"model": "gpt-6-astra", "reasoning_effort": "high"}
+    },
+    "claude": {
+      "leader_recommendation": "Opus",
+      "author": {"model": "fable", "reasoning_effort": "xhigh"},
+      "reviewer": {"model": "fable", "reasoning_effort": "high"}
+    },
+    "worker": {"frontier_allowed": false}
+  },
+  "activity_matrix": {
+    "specify": {"required": ["reviewer"], "author_for_new_how": true, "leader": ["setup", "load_handoff", "record_output"]},
+    "plan": {"required": ["author", "reviewer"], "frontend": ["design_author", "design_reviewer"], "leader": ["resolution", "setup", "gates", "record", "present_preview"]},
+    "checklist": {"required": ["reviewer"], "leader": ["deterministic_check"]},
+    "tasks": {"required": ["author", "reviewer"], "leader": ["setup", "format_check"]},
+    "analyze": {"required": ["reviewer"], "author_for_redesign": true, "leader": ["deterministic_check"]},
+    "partition": {"required": [], "leader": ["parse", "group", "validate_dag"]},
+    "implement-parallel": {"required": ["workers"], "author_for_new_decision": true, "reviewer_for_judgment": true, "leader": ["dispatch", "receipts", "mechanical_apply"]},
+    "converge": {"required": ["reviewer"], "author_for_additional_plan": true, "leader": ["diff", "ancestry", "mechanical_reconcile"]},
+    "verify": {"required": ["reviewer_if_judgment"], "leader": ["run_gates", "collect_evidence"]},
+    "review": {"required": ["reviewer_independent_of_all_authors"], "leader": ["record_findings", "record_verdict"]},
+    "ship": {"required": ["author_and_reviewer_for_new_judgment", "human_authorization"], "leader": ["canonical_skill_effects"]}
+  },
+  "interview": {"activity_scope": "interview", "step_id": null, "plan_only_stop": true, "roles": ["author", "reviewer"]},
+  "capabilities": {
+    "required_before_payload": ["session_identity", "effective_model", "effective_reasoning_effort", "close_confirmation", "separate_bootstrap_transport"],
+    "block_on": ["missing", "unproven_alias", "incomplete_observation", "shared_bootstrap_and_payload"]
+  },
+  "classification": {
+    "sources": ["handoff", "du", "plan_context"],
+    "platform_devops_without_surface": "NOT_APPLICABLE",
+    "contradiction": "BLOCKED",
+    "frontend_gate": ["html_preview", "png_captures", "byte_manifest", "independent_design_author", "independent_design_reviewer", "human_approval_current_digest"]
+  },
+  "task_contract": {
+    "marker": "grill-task-files:v1",
+    "result_per_writing_task": true,
+    "result_must_be_declared_in_files": true,
+    "phase_order": "workers_then_read_only_or_deferred",
+    "no_workers": "PARTITION-NO-WORKERS",
+    "adoption_required": true
+  },
+  "references": {
+    "protocol": {"path": "plugin/skills/grill-with-docs/references/agent-orchestration.md", "sha256": "sha256:c42b59d025becab2c6e0d493c2262cfe1ad40e632665367d4b4ad877a0225dab"},
+    "task_files_template": {"path": "plugin/skills/grill-with-docs/assets/task-files.v1.template.md", "sha256": "sha256:1f6d1d5ced9a10b21245f619b50bcb5f4bef5b00de4af8b011aea6584d45449f"}
+  },
+  "presentation": {
+    "schema": "grill-gwd-presentation/v1",
+    "component": "i-have-adhd@i-have-adhd",
+    "minimum_version": "0.3.0",
+    "approved": [{"version": "0.3.0", "skill_sha256": "sha256:3170b16ace00aecb0dd7feb54c0b5aa642e7502acda06ecd24fd89a11c7127e9"}],
+    "loader": "gwd-reference/v1",
+    "scope": "gwd",
+    "application": {"default": "active", "suspend_command": "stop adhd mode", "new_session": "active", "out_of_scope": "out_of_scope"},
+    "invariants": ["read_full_approved_bytes", "preserve_higher_instructions", "preserve_requested_content", "preserve_ponytail", "no_upstream_invocation", "no_global_flag", "no_global_config_or_cache_write"]
+  }
+}
diff --git a/plugin/skills/grill-with-docs/assets/task-files.v1.template.md b/plugin/skills/grill-with-docs/assets/task-files.v1.template.md
new file mode 100644
index 0000000..8650b7a
--- /dev/null
+++ b/plugin/skills/grill-with-docs/assets/task-files.v1.template.md
@@ -0,0 +1,22 @@
+<!-- grill-task-files:v1 -->
+
+# Tasks: <feature>
+
+## Phase 1: <phase name>
+
+- [ ] T001 <task description>
+  Files: ["path/to/file", "specs/<feature>/implement/T001.tasks.json"]
+  Result: "specs/<feature>/implement/T001.tasks.json"
+
+## Phase 2: <phase name>
+
+- [ ] T002 <read-only task description>
+  Files: []
+
+## Authoring rules
+
+- Declare `Files:` immediately after every checklist task as a one-line JSON array of strings.
+- A task with writes declares exactly one `Result:` immediately after `Files:`; its path is `specs/<feature>/implement/<task_id>.tasks.json` and is also present in `Files:`.
+- A read-only task declares `Files: []` and no `Result:`. It creates no worker grant or fallback scope.
+- Keep every task in its declared numeric phase. `Files` conflicts are grouped within that phase; phases remain barriers.
+- Results and phase-ordered deferred/read-only handling apply only after the candidate adopts `task-files/v1`; historical tasks retain their sealed protocol.
diff --git a/plugin/skills/grill-with-docs/references/agent-orchestration.md b/plugin/skills/grill-with-docs/references/agent-orchestration.md
new file mode 100644
index 0000000..4f2c4f7
--- /dev/null
+++ b/plugin/skills/grill-with-docs/references/agent-orchestration.md
@@ -0,0 +1,27 @@
+# Suplemento de orquestração de agentes v1
+
+Este suplemento integra `grill-agent-orchestration/v1` à entrada canônica já resolvida. Ele não é uma skill, alias, macroetapa ou substituto do registry: o líder recebe o contexto e invoca a skill canônica aplicável com estas instruções específicas.
+
+## Contexto de invocação
+
+O contexto devolve o entrypoint canônico, o registry hash, esta política e template com seus hashes, classificação, atividades exigidas e apresentação. Ler o contexto não é invocar a skill. O core verifica admissibilidade e os especialistas produzem artefatos; o líder mantém coordenação e evidência.
+
+## Autoria, revisão e matriz
+
+Use a matriz da política para as onze etapas: `specify`, `plan`, `checklist`, `tasks`, `analyze`, `partition`, `implement-parallel`, `converge`, `verify`, `review` e `ship`. Uma entrevista pré-ciclo usa `activity_scope=interview` e `step_id=null`; ela não cria uma macroetapa. Autorias novas requerem autor técnico xhigh e julgamentos requerem revisor high independente. O líder pode executar somente o trabalho mecânico listado; workers continuam não-frontier.
+
+## Tasks, Files e Result
+
+Para o contrato adotado da candidata, entregue ao autor o template `task-files/v1`. Cada tarefa declara `Files:` JSON e, quando escreve, um `Result:` explícito que também esteja em `Files:`. Não infira paths, não acrescente sidecar de node e não use `FEATURE_WIDE`. Resultados por tarefa substituem a receita histórica de sidecar por node somente neste contrato adotado; tasks históricas e DAGs selados não são reinterpretados.
+
+Agrupe conflitos somente dentro da fase. Em cada fase, converja workers e depois aceite atividades read-only/deferred na ordem das tasks antes de avançar. Fase sem worker é resolvida nessa posição. Activities vinculam task ID, fase, fingerprint e DAG; aceite positivo é necessário para ultrapassar a barreira. Se não houver tarefa despachável, `partition-emit` retorna `PARTITION-NO-WORKERS` antes de admissão, DAG-VALID, checkpoint ou worker fictício.
+
+## Capabilities e classificação
+
+Verifique capabilities observadas antes de enviar payload técnico: identidade, modelo/esforço efetivos, encerramento e transporte bootstrap/payload separado. Ausência, alias não comprovado ou observação incompleta bloqueiam; configuração solicitada não prova execução. Classifique frontend por handoff, DU e PLAN-CONTEXT coerentes. `platform-devops` sem superfície é `NOT_APPLICABLE`; classificação contraditória não pode contornar o gate visual. Frontend exige preview HTML, capturas PNG, manifest, autor/revisor independentes e aprovação humana do digest corrente antes de `tasks`.
+
+## Apresentação local GWD
+
+No fluxo GWD, o bootstrap resolve a instalação efetiva e lê integralmente a referência aprovada `i-have-adhd@i-have-adhd` antes da primeira resposta de trabalho. A apresentação é local ao fluxo/projeto GWD, preserva instruções superiores, conteúdo solicitado e Ponytail; não invoca a skill upstream, não cria flag global e não edita cache/configuração global. `stop adhd mode` suspende somente a apresentação na sessão comprovada; nova sessão volta ao default ativo. Retomada, mudança de runtime/incarnation e compactação revalidam contexto; suspensão válida mantém `work_ready`, mas não `use_ready` nem `functional_verified`.
+
+O evento de leitura precisa ser observado na mesma sessão, geração, hash e escopo. Enumeração, hash textual ou autorrelato não bastam. A referência carregada organiza apresentação; ela não substitui a skill canônica invocada, não altera JSON/código/evidência e não prova por si só comportamento do modelo.
diff --git a/specs/030-agent-orchestration/implement/p01-a.tasks.json b/specs/030-agent-orchestration/implement/p01-a.tasks.json
new file mode 100644
index 0000000..4214c40
--- /dev/null
+++ b/specs/030-agent-orchestration/implement/p01-a.tasks.json
@@ -0,0 +1,12 @@
+{
+  "worker": "p01-a",
+  "feature": "030-agent-orchestration",
+  "completed": ["T001"],
+  "tasks": {
+    "T001": {
+      "status": "done",
+      "summary": "Added the versioned agent-orchestration policy, task-files/v1 authoring template, and supplemental protocol. The policy fixes local protocol/template refs and SHA-256 values, role/runtime and eleven-step/interview matrix, capability and classification gates, and approved i-have-adhd 0.3.0 bytes without copying upstream content. The protocol keeps canonical skill invocation distinct from delivered context and limits Files/Result and phase-ordered deferred semantics to an adopted candidate contract.",
+      "evidence": "PYTHONDONTWRITEBYTECODE=1 python3 focused stdlib policy/template/protocol validation: strict JSON and duplicate-key rejection; local references and SHA-256 values verified; template Result rule and protocol invocation distinction verified."
+    }
+  }
+}
