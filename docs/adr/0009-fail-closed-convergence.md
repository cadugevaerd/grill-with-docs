# Fail-closed convergence

A etapa `converge` integra somente mudanças limpas e validadas. Conflitos Git ou sobreposição de escopo declarado criam `INTEGRATION_CONFLICT`, com os nós afetados e diagnóstico; o Loop não resolve automaticamente esses conflitos nem reescreve o Execution DAG para contorná-los.
