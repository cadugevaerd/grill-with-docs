# Review block without a macro loop

O workflow V3 não define uma etapa canônica de reparo depois de `review`. Assim, uma Independent Review reprovada produz `REVIEW_BLOCKED` com diagnóstico no chat principal, sem reabrir ou repetir macroetapas automaticamente. Um ciclo de reparo exige uma ampliação futura e explícita do protocolo.
