# Status timeout version bump — leader receipt

- T017: `README.md` atualizado de `v5.3.0` para `v5.3.1`.
- T018: `CHANGELOG.md` recebeu uma única seção `## 5.3.1`, descrevendo a
  correção do falso `STATUS-TIMEOUT`, o cache de probes Git por
  worktree/repositório e o timeout público de 30s.

Arquivos alterados pelo leader sobre o HEAD coordenador após a convergência
integral de T010–T016.
